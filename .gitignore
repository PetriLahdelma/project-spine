# deps
node_modules/

# build
dist/
*.tsbuildinfo

# test
coverage/

# env
.env
.env.local
.env.*.local

# editor
.DS_Store
.vscode/
.idea/
.history/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# generated public metadata snapshots - the Next route source stays tracked
public-site-analytics.v1.json
!site/app/schemas/public-site-analytics.v1.json/
!site/app/schemas/public-site-analytics.v1.json/route.ts

# project-spine compile outputs at repo root — the tool's own output.
# Regenerate with `spine compile`. A committed snapshot lives in docs/sample-output/.
/.project-spine/
/AGENTS.md
/CLAUDE.md
/.github/copilot-instructions.md
/.cursor/rules/project-spine.mdc

# OS
Thumbs.db
.vercel
.env*.local

# maintainer-local tooling state — never commit
/.planning/
/.claude/
