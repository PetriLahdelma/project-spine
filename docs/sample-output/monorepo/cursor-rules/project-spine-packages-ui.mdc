---
description: Project Spine scoped guidance for "packages/ui"
globs: "packages/ui/**"
alwaysApply: false
---

# Project Spine — packages/ui

<!-- generated by Project Spine; edit `brief.md` or rerun `spine compile` rather than hand-editing -->

This scoped Cursor rule applies when editing `packages/ui`.

- Follow `@AGENTS.md` for the repo-wide operating contract.
- Review `@.project-spine/exports/qa-guardrails.md` before claiming completion.
- If framework or routing guidance is wrong for this workspace, re-run compile with `spine compile --brief ./brief.md --repo packages/ui`.
- Keep imports inside the workspace boundary unless a shared package owns the dependency.

Source of truth: `.project-spine/spine.json` (hash `5632a143e1b77025`).

<!-- spine:deterministic -->
